apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test
spec:
  volumeName: test
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10M